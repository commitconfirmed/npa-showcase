---
- hosts: eos
  gather_facts: false

  tasks:
  - name: Test connectivity
    eos_command:
      commands:
        - show version  
    register: version_details

  - name: Display result
    debug:
      var: version_details